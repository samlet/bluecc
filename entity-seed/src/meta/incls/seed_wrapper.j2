{%- for mod in modules -%}
use crate::models::{{mod-}}_types::*;
{% endfor %}

#[derive(Clone, Serialize, Deserialize, Debug)]
pub struct SeedRecords {
    pub items: Vec<SeedTypes>
}

#[derive(Clone, Serialize, Deserialize, Debug)]
pub enum SeedTypes {
{%- for ent in ents %}
    {{ent}}({{ent}}),
{%- endfor %}
}

